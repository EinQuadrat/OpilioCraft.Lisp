<Project>

    <PropertyGroup Label="UserMacros">
        <SolutionId>OpilioCraft.Lisp</SolutionId>
    </PropertyGroup>

    <PropertyGroup>

        <VersionPrefix>1.2.1</VersionPrefix>
        <Authors>Carsten Schäfer</Authors>
        <Copyright>Copyright (c) 2022-2024 by Carsten Schäfer</Copyright>
        <RepositoryUrl>https://github.com/EinQuadrat/OpilioCraft.Lisp.git</RepositoryUrl>
        <PackageTags>OpilioCraft;Lisp;FSharp</PackageTags>

        <AssemblyName>$(SolutionId).$(MSBuildProjectName)</AssemblyName>
        <RootNamespace>$(SolutionId).$(MSBuildProjectName.Replace(" ", "_"))</RootNamespace>

    </PropertyGroup>

</Project>
